/**
 * _islower is a function that will check for lowercase
 *
 * Return: 1 if c, o otherwise
 */

#include "main.h"

int _islower(int c)
{

	if (c >= 'a' && c <= 'z')
		return (1)
	else
		return (0);
}
