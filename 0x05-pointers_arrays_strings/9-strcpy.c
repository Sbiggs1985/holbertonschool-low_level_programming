#include "main.h"
/**
 * print_array - Prints elements
 * @a: The pointer
 * @n: The array quantity
 * Return: 
 */

void print_array(int *a, int n)
{
	int fi;

	for (f1 = 0; f1 < n; fi++)
	{
		if (f1 == 0)
		{
			printf("d", a[f1];
		}
		else
		{
			printf("%d", a[f1];
		}
		printf('\n');

