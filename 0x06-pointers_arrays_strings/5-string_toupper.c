#include "holberton.h"
/**
 * string_toupper - A function that changes lower of a string to upper
 * @n: Input used to return n
 * Return: capitalized string
 */
char *string_toupper(char *n)
{
	int pear = 0;

	while (str[pear])

	if (str[pear] >= 'a' && str[pear] <= 'z')
		str[pear] -= 32;
	pear++;
}
	return (str);
}
