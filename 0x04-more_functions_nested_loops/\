#include "main.h"
#include <stdio.h>
 /**
 * _lastdigit - Return 1 if c is uppercase
 * @c: we have declared the char c
 * return: 1 or 0
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
